# State

Currently managed client-side:
- Selected mood (local state, visual feedback before navigating)
- Random daily message (chosen on mount)

Future:
- User emotional history (back-end driven)
- Room/content state via API